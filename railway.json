{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "chmod -R 777 storage bootstrap/cache && mkdir -p database && touch database/database.sqlite && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/login",
        "restartPolicyType": "ON_FAILURE"
    }
}
